import { styled } from '..'

export const CentralizeContainer = styled('main', {
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  alignItems: 'center',
})

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  width: '1180px',
})

export const Banner = styled('div', {
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
  marginTop: '30px',
})
